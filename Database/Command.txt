
ALTER USER DEV_BIPLATFORM IDENTIFIED BY (yourpassword);
ALTER USER DEV_MDS IDENTIFIED BY (yourpassword);

ALTER USER DEV_BIPLATFORM ACCOUNT UNLOCK;
ALTER USER DEV_MDS ACCOUNT UNLOCK;

alter profile DEFAULT limit password_life_time UNLIMITED;

SQL> alter profile DEFAULT LIMIT SESSIONS_PER_USER UNLIMITED 
SQL> alter profile DEFAULT LIMIT CPU_PER_SESSION UNLIMITED 
SQL> alter profile DEFAULT LIMIT CPU_PER_CALL UNLIMITED 
SQL> alter profile DEFAULT LIMIT CONNECT_TIME UNLIMITED 
SQL> alter profile DEFAULT LIMIT IDLE_TIME UNLIMITED 
SQL> alter profile DEFAULT LIMIT LOGICAL_READS_PER_SESSION UNLIMITED 
SQL> alter profile DEFAULT LIMIT LOGICAL_READS_PER_CALL UNLIMITED 
SQL> alter profile DEFAULT LIMIT COMPOSITE_LIMIT UNLIMITED 
SQL> alter profile DEFAULT LIMIT PRIVATE_SGA UNLIMITED 
SQL> alter profile DEFAULT LIMIT FAILED_LOGIN_ATTEMPTS 10 
SQL> alter profile DEFAULT LIMIT PASSWORD_LIFE_TIME UNLIMITED 
SQL> alter profile DEFAULT LIMIT PASSWORD_REUSE_TIME UNLIMITED 
SQL> alter profile DEFAULT LIMIT PASSWORD_REUSE_MAX UNLIMITED 
SQL> alter profile DEFAULT LIMIT PASSWORD_LOCK_TIME 0.0106 
SQL> alter profile DEFAULT LIMIT PASSWORD_GRACE_TIME 7 
SQL> alter profile DEFAULT LIMIT PASSWORD_VERIFY_FUNCTION NULL; 



Check the BI_BIPLATFORM account status. 
SQL> SELECT username, account_status FROM dba_users WHERE username= `BI_BIPLATFORM';
USERNAME ACCOUNT_STATUS PROFILE 
-------------------- -------------------- --------------- 
BIPLATFORM LOCKED(TIMED) DEFAULT 


SQL> SELECT resource_name,resource_type,limit FROM dba_profiles WHERE profile='DEFAULT';

@ Create a profile. 
@ . 
@ SQL> CREATE PROFILE APPUSR_DEFAULT LIMIT 
@ 2 COMPOSITE_LIMIT UNLIMITED 
@ 3 SESSIONS_PER_USER UNLIMITED 
@ 4 CPU_PER_SESSION UNLIMITED 
@ 5 CPU_PER_CALL UNLIMITED 
@ 6 LOGICAL_READS_PER_SESSION UNLIMITED 
@ 7 LOGICAL_READS_PER_CALL UNLIMITED 
@ 8 IDLE_TIME UNLIMITED 
@ 9 CONNECT_TIME UNLIMITED 
@ 10 PRIVATE_SGA UNLIMITED 
@ 11 FAILED_LOGIN_ATTEMPTS UNLIMITED 
@ 12 PASSWORD_LIFE_TIME UNLIMITED 
@ 13 PASSWORD_REUSE_TIME UNLIMITED 
@ 14 PASSWORD_REUSE_MAX UNLIMITED 
@ 15 PASSWORD_VERIFY_FUNCTION NULL 
@ 16 PASSWORD_LOCK_TIME UNLIMITED 
@ 17 PASSWORD_GRACE_TIME UNLIMITED; 

SQL> ALTER USER BI_BIPLATFORM PROFILE appusr_default; 

ALTER USER BI_BIPLATFORM ACCOUNT UNLOCK; 


Login to database in linux
==========================

export ORACLE_HOME=/product/11.2.0/db_home1
export ORACLE_SID=orcl

cd /product/11.2.0/db_home1/bin
./sqlplus

enter user id and password

ON/OFF iptables

sudo su
/etc/init.d/iptables save
/etc/init.d/iptables stop
